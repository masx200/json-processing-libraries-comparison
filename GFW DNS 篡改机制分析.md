# **GFW DNS 篡改机制的深度技术分析：以 [www.youtube.com](https://www.youtube.com) 阻断为例**

## **I. 执行摘要 (Executive Summary)**

针对用户关于“GFW如何篡改DNS，特别是针对www.youtube.com的测试结果”的询问，本报告提供了一份详尽的、技术性的分析。中国的网络审查系统，通常被称为“防火长城”（Great Firewall, GFW），采用了一种高度精密且动态的域名系统（DNS）投毒（或称欺骗/Spoofing）机制，作为其内容过滤策略的核心组成部分。

**机制核心：** GFW在网络出口层面对用户发出的DNS查询流量进行深度包检测（DPI）。一旦发现查询目标域名（如www.youtube.com）位于黑名单内，审查系统会实时伪造一个虚假的DNS响应包。由于GFW节点在物理距离上远比境外合法DNS服务器更接近用户，其响应包能够通过网络竞速条件（Race Condition）抢先到达客户端，从而实现毒化。

**战略创新：** GFW的DNS投毒策略在国际网络审查实践中独树一帜。它放弃了传统审查方法中常见的明确阻断信号，例如返回“域名不存在”（NXDOMAIN）或私有IP地址（如 $10.0.0.0/8$）。相反，GFW选择使用大规模、动态且在全球范围内可路由的真实第三方IP地址（如属于Facebook、Twitter的地址）作为诱饵响应 1。这一策略的主要目的是最大限度地混淆检测，使得自动化规避工具难以通过简单的IP地址黑名单来识别审查行为 1。

**网络签名与应用：** 当用户查询www.youtube.com被成功投毒后，客户端操作系统会缓存并尝试连接收到的虚假IP地址。由于这些诱饵IP地址并非由GFW控制且不会响应SYN请求，连接尝试将依赖于标准的TCP协议栈进行指数退避重传（Retransmission Timeout, RTO）。最终，这导致客户端在约10秒的延迟后报告连接失败。针对像www.youtube.com这类被永久封锁的站点，DNS投毒是首要的阻断手段，确保用户在建立任何更高层级（如加密的HTTPS）连接之前即被误导和阻断。

## **II. 背景与网络审查生态：DNS毒化的战略定位**

### **II. A. GFW在国家网络边界的分布式架构**

GFW并非单一的物理设备，而是一个庞大、分布式、多层次的深度包检测（DPI）与过滤系统。其功能部署在全国多个关键网络节点，特别是在国际互联网出口（AS边界）和主要的城域网交换点。在这一架构中，DNS拦截是效率最高的审查手段之一，因为它发生在连接建立的最早期阶段。

DNS毒化的目标流量主要是发往境外或不受信任的递归解析器的UDP/53端口查询请求。通过在这些关键网络路径上设置过滤点，GFW能够有效地监控和拦截几乎所有跨越国境的DNS查询。

### **II. B. 全球DNS审查策略比较与GFW的差异化**

DNS审查在全球范围内被广泛应用，但各国的实施策略存在显著差异。

**传统审查模式** 许多国家或实体采用直接且易于识别的阻断信号。例如，一些国家和地区，如巴林、科威特、伊朗或也门，倾向于将用户重定向到明确的阻断页面（Blockpages），以告知用户内容已被审查。在技术层面，这通常通过返回一个专门用于阻断页面的固定IP地址实现。另一些审查者则可能返回 **NXDOMAIN**（表示域名不存在）响应，或使用RFC 1918定义的**内部私有IP地址范围**（例如 $10.0.0.0/8$）作为伪造记录 1。这些传统方法的共同特点是：审查信号是明确的，并且易于被自动化工具识别和列入黑名单。

**GFW的独特性** GFW的策略则是一种更具欺骗性的高级手段。该系统战略性地采用**全球可路由的真实第三方IP地址**作为虚假DNS响应 1。这些IP地址属于国际上知名且活跃的公司，如Facebook、Dropbox和Twitter等 1。

这种对全球可路由IP地址的使用，将检测审查的难度提高了几个数量级。通过返回一个格式合法、且归属于知名实体的IP地址，审查信号被有效地“合法化”和混淆。监测系统和规避工具无法简单地依靠IP地址的私有性或重复性来识别审查，而必须依赖更复杂的、基于时序或行为模式的分析，例如通过测量虚假响应的往返时间（RTT）是否异常地快，来区分审查行为和合法的DNS解析结果 1。这种策略反映出GFW在网络防御和反规避方面投入了巨大的资源，并将其审查行为提升到了一种主动混淆（Active Evasion）的水平。

## **III. GFW DNS 篡改的核心机制：包伪造与竞速条件**

GFW的DNS投毒是一种高精度的、实时（on-the-fly）的网络注入攻击，其成功依赖于对网络协议的深刻理解和极低的响应延迟。

### **III. A. 流量监控与黑名单匹配**

审查过程始于GFW部署在网络关键路径上的DPI设备。这些设备持续监听流经的全部网络流量。当用户发起DNS查询时，GFW会立即检查UDP/53端口的查询负载，将其与内部的、动态更新的黑名单域名库进行匹配。例如，当检测到对www.youtube.com的查询请求时，投毒过程随即被触发。

### **III. B. 报文伪造的精密要求与竞速**

DNS投毒攻击的核心在于**竞速机制**（Race Condition）。合法DNS解析器，特别是那些位于境外的公共解析器，通常会有较高的网络延迟。GFW必须在用户请求到达境外合法服务器并返回响应之前，或者更常见的是，在合法响应到达客户端之前，将伪造的响应包注入网络并确保其被客户端接受。

要赢得这场毫秒级的竞速，GFW过滤节点必须在网络架构中被部署得尽可能靠近用户，同时具备极高的包处理性能。这解释了为什么GFW的DNS投毒响应往往具有极低的往返时间（RTT）。

报文伪造的精密性至关重要：

1. **源IP仿冒：** 伪造的响应包的源IP地址必须精确地仿冒用户最初查询的目标DNS服务器的IP地址。如果源IP不匹配用户的原始查询目标，客户端的操作系统网络栈会直接丢弃该响应。  
2. **交易ID匹配 (Transaction ID):** DNS查询使用一个16位的交易ID（Transaction ID, TXID）来匹配请求和响应。这是伪造成功的关键。伪造包必须包含与原始查询完全一致的TXID。如果TXID不匹配，客户端将认为该响应是针对另一个查询的，并将其拒绝。

### **III. C. TTL策略与缓存毒化效果**

在成功伪造的A记录响应中，GFW通常会设置一个较高的生存时间（Time-to-Live, TTL）值，例如3600秒（一小时）。

这种TTL策略具有双重效益。首先，较高的TTL值使得客户端或任何中间的本地DNS解析器会长时间缓存该虚假记录。其次，在缓存未过期期间，GFW无需为该用户再次查询同一域名时重复进行注入操作。这显著降低了GFW持续监控和注入的运营负载，优化了审查系统的资源消耗。

## **IV. 诱饵IP地址策略的演变与反制绕过**

GFW的DNS投毒策略并非一成不变，而是根据外部规避工具的演进而不断升级，体现了网络攻防的持续军备竞赛。

### **IV. A. IP池的历史性扩张与适应性**

GFW的诱饵IP策略经历了从固定到动态的根本性转变：

**初期阶段（约2010年至2014年初）：** 在这一时期，GFW使用的诱饵IP地址池规模非常小，且固定不变。2010年，观测到的虚假IP地址仅有9个；到2011年增加到28个；至2014年，该数字增长至174个 1。由于使用的IP地址子集固定且规模有限，反审查社区迅速开发出“反毒化工具” 2，这些工具通过简单识别这些已知的固定假IP地址，并自动绕开它们，从而使初期的DNS投毒效果大打折扣 2。

**战略转折点（2015年升级）：** 面对基于黑名单的规避工具的有效性，GFW在2015年初进行了重大的策略升级 2。系统彻底放弃了使用小型、固定IP池的做法，转而采用一种全新的策略：**将用户重定向到随机的、真实的、全球可路由的IP地址** 2。近期测量显示，该诱饵IP地址池的规模已超过1.5K个 1，且仍在持续增长。

这种指数级的增长表明GFW拥有一个庞大且持续更新的真实、可路由IP地址数据库，需要专用的自动化系统来管理其在分布式审查架构中的部署和动态策略更新。这种对IP地址池规模和动态性的巨大投入，显示了审查系统对维持其阻断有效性的决心。

### **IV. B. 战略目的：混淆与反制绕过**

采用大规模、动态的真实IP地址作为诱饵，其战略目的在于反制自动化检测和规避工具：

1. **混淆检测：** 当返回的IP地址是谷歌、Facebook或Twitter等知名公司的合法IP时，检测系统难以仅凭IP地址的属性（如是否在私有地址空间或是否固定重复）来判定它是否为审查信号 1。这极大地提高了GFW监测平台的复杂性。  
2. **瓦解反毒化工具：** 这一升级直接针对了依赖IP黑名单进行规避的早期工具 2。由于诱饵IP现在是随机且动态变化的，试图维护一个包含所有虚假IP的静态黑名单几乎不可能，迫使规避措施转向更高级的方法，如基于加密隧道或时序分析。

#### **演进中的GFW DNS审查策略对比**

为了清晰地展示策略的演变，下表对比了GFW在不同时期采用的DNS审查策略。

GFW DNS 审查策略对比分析

| 时间段 | Decoy IP 类型 | IP 池规模 (估算) | 主要反审查挑战 | GFW 战略目标 |
| :---- | :---- | :---- | :---- | :---- |
| 2000s \- 2014年初 | 固定、不可路由或非目标地址 | 极小 (≤ 200\) 1 | 易于通过静态规则识别和绕过 2 | 简单阻断，低资源消耗 |
| 2015年至今 | 动态、全球可路由的第三方地址 | 持续增长 (1.5K+) 1 | 难以通过 IP 识别；复杂化检测，需要时序或行为分析 | 混淆检测，反制绕过工具 2 |

## **V. 网络后果与流量去向分析**

当DNS投毒成功，例如针对www.youtube.com的查询被虚假IP响应替换后，客户端尝试连接伪造的IP地址，这引发了一系列可预测的网络后果。

### **V. A. TCP连接失败的精确时序分析**

一旦客户端收到并接受了被投毒的IP地址，它将尝试对该地址发起TCP连接建立流程，即三次握手（SYN, SYN-ACK, ACK）。由于这个IP地址属于一个与目标服务（YouTube）无关的境外服务器（例如，Facebook或Twitter的服务器），它将不会响应客户端发出的SYN请求。

GFW的策略优势在于其选择了一种“被动阻断”模型：它不主动向客户端发送TCP RST（Reset）报文来立即终止连接。相反，GFW依赖于标准的客户端操作系统TCP协议栈来处理连接失败，从而节省了审查系统主动维护和发送RST包所需的资源。

客户端的TCP协议栈在未收到SYN-ACK响应时，会遵循指数退避重传（RTO）算法，逐步增加重试的间隔时间。在多数现代操作系统中，这一重传序列通常遵循一个标准的时间模式，导致连接尝试最终在约10秒左右超时失败。这种可预测的10秒延迟，正是DNS投毒成功后，应用程序（如浏览器）报告连接失败的经典网络签名。

### **V. B. TCP/IP 连接建立失败时序分析**

下表详细描述了客户端在连接被投毒的IP地址时，TCP重传和超时的标准过程，解释了为什么连接失败的时长约为10秒：

TCP/IP 连接建立失败时序分析

| 阶段 | 协议行为 | RTO (指数退避) | 总计累计时间 (估算) | 网络依赖机制 |
| :---- | :---- | :---- | :---- | :---- |
| 0 | 接收虚假 A 记录 | 瞬时 | 0s | GFW 注入 |
| 1 | 客户端 SYN 1 (发送) | 1.0 s | 1.0 s | 标准 TCP 规范 |
| 2 | 客户端 SYN 2 (重传) | 2.0 s | 3.0 s | 标准 TCP 规范 |
| 3 | 客户端 SYN 3 (重传) | 4.0 s | 7.0 s | 标准 TCP 规范 |
| 4 | 客户端 SYN 4 (重传) | 3.0 s | $\\approx 10.0$ s | 标准 TCP 规范 |
| 5 | 应用层 | N/A | 10+ 秒 | 浏览器或应用程序报告连接失败 |

### **V. C. 流量的最终去向：DDoS附带效应**

关于发送到虚假IP地址的流量的最终去向，分析表明这些流量并不会被GFW系统简单地“黑洞”（Black-hole）处理。由于GFW故意选择了**全球可路由的真实第三方IP地址** 1，客户端发出的TCP SYN请求会被视为合法的出境网络流量，并通过国际路由被转发到其真正的目的地，例如位于美国的Facebook或Twitter服务器。

这种主动路由的行为产生了一个重大的附带效应：**反射型分布式拒绝服务（DDoS）攻击**。当数百万用户尝试访问被阻断的站点（如www.youtube.com）时，所有查询都被重定向到无辜的第三方目标IP地址。目标服务器因此被大量的、非故意的、不完整的TCP SYN请求洪流淹没。无论这种DDoS效应是GFW的战略意图（旨在惩罚或间接攻击其黑名单上的境外服务）还是仅仅是其高效混淆策略的副作用，其结果都是对全球互联网基础设施构成了持续的、非故意的攻击 2。

## **VI. 审查基础设施的洞察与脆弱性**

对GFW审查机制的分析，必须考虑到其内部运营的复杂性和潜在的脆弱性。

### **VI. A. 基于数据泄露的内部视角**

2025年发生的大规模网络基础设施数据泄露事件，为研究人员提供了窥探GFW内部运作的罕见机会 3。据报告，泄露的资料包括超过10万份文件，涉及内部源代码、配置日志、工单系统（Jira, Confluence）记录、以及用于对抗规避手段的技术手册 3。

这些内部文件揭示了关键的运营细节：

1. **主动对抗规避工具：** 泄露数据证实，GFW工程师正在积极测试其过滤逻辑，专门针对VPN、Tor以及TLS/SSL指纹识别等高级规避方法 3。这表明审查系统对不断演进的加密隧道和规避技术保持着高度的警惕和投入。  
2. **配置与部署挑战：** 泄露的IP日志和抓包数据显示了审查系统在实际运行中出现故障的关键时刻，暴露了GFW分布式执行模型的固有**脆弱性** 3。观测到的“跨境泄漏路线”允许外部IP在一段时间内建立未过滤的会话，以及规则应用中的“延迟” 3，都证明了管理一个庞大、动态且复杂的过滤策略（例如管理1.5K+诱饵IP池）在运维上存在巨大的难度和不一致性。

### **VI. B. 分布式模型的运维挑战**

GFW的分布式结构虽然提供了广阔的覆盖范围，但同时也带来了内在的运维挑战。策略的动态性（如频繁更新黑名单和IP诱饵池）使得在全国范围内部署和同步规则变得异常复杂。

审查策略在不同地理位置和时间上的执行不一致性是该系统的主要脆弱性。规则部署的延迟或配置错误可能导致某些DNS查询暂时逃脱审查，而用户连接的成功率将取决于其所在地的GFW节点的实时同步状态。这种不一致性为监测和规避研究提供了重要的切入点。

## **VII. 检测、监测与规避策略**

面对GFW不断升级的动态DNS投毒策略，检测和规避方法也必须随之进化。

### **VII. A. 纵向监测与特征识别**

由于IP地址本身已被用作混淆工具，传统的IP黑名单方法已失效 2。因此，持续、大规模的纵向监测平台（如GFWatch）变得至关重要 1。这些平台用于实时追踪不断变化的审查策略和动态的诱饵IP池规模。

高级检测必须转向**时序和协议特征分析**：

1. **往返时间（RTT）偏差分析：** 虚假响应来自部署在境内的GFW节点，其RTT通常显著低于来自境外合法DNS服务器的响应时间。这种时序差异是识别投毒行为的可靠特征。  
2. **多源查询：** 同时向多个不同国家和地区的DNS解析器发送查询，并比较返回的A记录和响应时间。如果境外解析器返回一个合法IP，而境内观测点返回一个已知的诱饵IP（如Facebook或Twitter的地址），即可确认为审查投毒。

### **VII. B. 规避技术的升级与挑战**

GFW在2015年升级后，传统基于固定黑名单的规避工具几乎被完全瓦解 2。现代规避技术必须采用加密和隧道化手段，将DNS查询从GFW的DPI检查范围中移除。

**现代解决方案包括：**

1. **加密DNS协议（DoH/DoT）：** DNS over HTTPS (DoH) 和 DNS over TLS (DoT) 将DNS查询封装在标准的TLS/HTTPS加密隧道中。这使得GFW的DPI系统无法在传输层读取查询的域名负载，从而无法触发交易ID匹配和后续的投毒响应。  
2. **VPN和加密隧道：** 通过将整个用户连接（包括DNS查询）重定向到境外安全节点，用户完全绕过了位于国家边界的GFW DNS拦截点。

需要指出的是，这场军备竞赛仍在继续。GFW的内部文件显示，为了应对加密DNS和隧道化，审查系统已将焦点从网络层（Layer 3/4）和应用层（Layer 7 DNS Payload）转移到识别加密连接的**TLS/SSL指纹**。这意味着GFW正试图通过分析加密隧道的元数据（如TLS握手特征）来识别并阻断那些被用于规避目的的加密流量。

## **VIII. 结论与未来展望 (Conclusion and Future Outlook)**

GFW的DNS篡改机制是高度技术化和策略性进化的网络审查武器。通过利用网络竞速条件和高精度的报文伪造（包括精确匹配交易ID），它实现了高效的初始阻断，特别是针对www.youtube.com这类被永久封锁的国际服务。

**核心结论：** GFW DNS投毒机制的最大战略特点是其采用了大规模、动态且在全球可路由的真实第三方IP地址作为诱饵。这种策略的核心目标在于**混淆检测**，使监测和规避工具无法简单地通过静态IP规则来识别审查行为。成功的DNS投毒后，客户端面临的约10秒连接超时是标准TCP指数退避重传的必然结果。此外，这种策略产生的不可避免的副作用是，它对被盗用IP地址的合法所有者（如Facebook）构成了持续的、大规模的、非故意的分布式拒绝服务（DDoS）攻击。

**未来趋势：** 预计GFW将继续投入资源，致力于应对新兴的加密规避技术。未来的审查重点将从传统的DNS内容检测，转向利用深度学习和流量指纹识别技术来识别和阻断加密隧道（如DoH、DoT和定制VPN协议）。

因此，网络自由的维护者和规避技术开发者必须持续进行大规模、纵向的监测，并采用基于时序分析和多源验证的动态检测方法，以适应这种不断演进和自我优化的审查机制。这场网络攻防战的核心，已经从简单的协议阻断，转向了更复杂的流量指纹识别和行为分析领域。

#### **引用的著作**

1. How Great is the Great Firewall? Measuring China's DNS Censorship \- Stony Brook CS, 访问时间为 十二月 3, 2025， [https://www3.cs.stonybrook.edu/\~mikepo/papers/gfwatch.sec21.pdf](https://www3.cs.stonybrook.edu/~mikepo/papers/gfwatch.sec21.pdf)  
2. Cyber Kung-Fu: The Great Firewall Art of DNS Poisoning \- crowdstrike.com, 访问时间为 十二月 3, 2025， [https://www.crowdstrike.com/en-us/blog/cyber-kung-fu-great-firewall-art-dns-poisoning/](https://www.crowdstrike.com/en-us/blog/cyber-kung-fu-great-firewall-art-dns-poisoning/)  
3. Inside the Great Firewall Part 1: The Dump \- DomainTools Investigations | DTI, 访问时间为 十二月 3, 2025， [https://dti.domaintools.com/inside-the-great-firewall-part-1-the-dump/](https://dti.domaintools.com/inside-the-great-firewall-part-1-the-dump/)